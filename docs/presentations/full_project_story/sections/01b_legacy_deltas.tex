% ---------------------------------------------------------------- Legacy -> current, model and objective
\begin{frame}{From the legacy pipeline to the current system: model and objective}
  \centering
  \scriptsize
  \renewcommand{\arraystretch}{1.12}
  \begin{tabular}{@{}l p{4.9cm} p{6.1cm}@{}}
    \toprule
    & \textbf{legacy pipeline} & \textbf{current system} \\
    \midrule
    backbone            & UNet5Lv3, one fixed architecture            & registry of 20 backbones, one contract \\
    standard trunk      & the same UNet                               & UNet-skip \\
    conv block          & conv--ReLU $\times 2$, biased               & conv--norm--activation, all configurable \\
    normalization layers& none                                        & batch / instance / group \\
    dropout             & none                                        & configurable, 0.15 standard \\
    weight init         & PyTorch default                             & default / Kaiming / Xavier \\
    \addlinespace[3pt]
    model form          & single network                              & dual: parameter trunk + existence trunk \\
    trunk budget        & \textcolor{soft}{---}                       & 90/10 parameter / existence loss split \\
    output head         & one $1{\times}1$ conv                       & conv / multihead / per-Gaussian / set-pred \\
    standard head       & \textcolor{soft}{---}                       & set prediction: $K$ PixelMLP heads + gate \\
    \addlinespace[3pt]
    slots $K$           & 2, hard-coded                               & configurable, studied at $K{=}2$ and $K{=}5$ \\
    slot identity       & positional, ground first                    & Hungarian matching, sorted-GT control \\
    presence signal     & the \emph{prediction} ($\hat a_2, \hat w_2$)& the \emph{ground truth} activity mask \\
    missed scatterer    & never penalised, collapse optimum           & penalised, per-slot presence balance \\
    imbalance handling  & none                                        & active normalization, amp focal option \\
    \addlinespace[3pt]
    parameter loss      & masked MSE, group means                     & param L1 standard, Huber / MSE options \\
    curve losses        & none                                        & MSE / L1 / Huber / Charbonnier / cosine \\
    physics terms       & none                                        & 5 terms, warmup $\to$ complete curriculum \\
    \bottomrule
  \end{tabular}
\end{frame}

% ---------------------------------------------------------------- Legacy -> current, data and normalization
\begin{frame}{From the legacy pipeline to the current system: data and normalization}
  \centering
  \scriptsize
  \renewcommand{\arraystretch}{1.12}
  \begin{tabular}{@{}l p{4.9cm} p{6.1cm}@{}}
    \toprule
    & \textbf{legacy pipeline} & \textbf{current system} \\
    \midrule
    input stack         & 4 secondary $|A|$ + 4 ifg $\varphi$         & + primary $|A|$, optional DEM \\
    representations     & fixed                                       & magnitude / phase / real-imag axis per group \\
    targets             & 2-Gaussian fit, one RAT file                & GPU-fitted $K$-Gaussian labels + presence \\
    \addlinespace[3pt]
    input $|A|$ scaling & $\log(1{+}m)$, unscaled                     & robust-IQR log1p, fitted \\
    input $\varphi$ scaling & fixed $[-\pi,\pi] \to [0,1]$            & fixed $\div\,\pi$ / fitted, per channel \\
    target scaling      & hand-set min-max constants                  & fitted per channel over active pixels \\
    $\mu$ channels      & min-max                                     & z-score \\
    $a, \sigma$ channels& min-max                                     & robust-IQR log1p (heavy tails) \\
    statistics source   & the whole file                              & train region only \\
    output range        & unconstrained                               & physical clamp on $\hat a, \hat\mu, \hat\sigma$, leaky \\
    \addlinespace[3pt]
    split               & random patch shuffle 80/10/10               & contiguous azimuth bands \\
    split leakage       & neighbours cross splits                     & none by construction \\
    patch shape         & $64 \times 64$, square only                 & rectangular (az, rg), standard $64{\times}32$ \\
    stride              & fixed                                       & per-axis, standard $32{\times}16$ \\
    patch size choice   & unstated                                    & derived floor / ceiling, confirmed by sweep \\
    augmentation        & none                                        & flips standard; rot90, noise optional \\
    \bottomrule
  \end{tabular}
\end{frame}

% ---------------------------------------------------------------- Legacy -> current, optimization and tooling
\begin{frame}{From the legacy pipeline to the current system: optimization and tooling}
  \centering
  \scriptsize
  \renewcommand{\arraystretch}{1.12}
  \begin{tabular}{@{}l p{4.9cm} p{6.1cm}@{}}
    \toprule
    & \textbf{legacy pipeline} & \textbf{current system} \\
    \midrule
    optimizer           & Adam                                        & AdamW \\
    parameter groups    & one                                         & encoder / bottleneck / decoder / head \\
    learning rate       & $10^{-5}$, flat                             & per group, benchmarked \\
    weight decay        & 0                                           & per group \\
    schedule            & constant                                    & cosine annealing \\
    warmup              & none                                        & lr warmup, 200 steps \\
    batch scaling       & none                                        & lr scaled with batch size \\
    clipping            & none                                        & gradient clipping, fixed + adaptive \\
    \addlinespace[3pt]
    epochs              & 300, fixed                                  & early stopping, best-checkpoint restore \\
    checkpointing       & one save at the end                         & per-epoch validation, resumable state \\
    sanity gates        & none                                        & overfit gate, loss-scale probe \\
    guards              & none                                        & non-finite abort, VRAM reservation \\
    precision           & fp32                                        & AMP option, EMA option \\
    \addlinespace[3pt]
    evaluation          & one final MSE print                         & benchmark suite, physical MAEs \\
    diagnostics         & none                                        & occupancy, count accuracy, per-slot stats \\
    experiment control  & hand-edited scripts                         & config CLI, web console, GPU pools \\
    multi-run           & manual                                      & multi-seed fan-out, queued experiments \\
    reproduction        & \textcolor{soft}{---}                       & legacy mode: one click rebuilds this column \\
    \bottomrule
  \end{tabular}
\end{frame}
