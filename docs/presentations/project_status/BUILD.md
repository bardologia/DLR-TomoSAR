# Build

Compile the deck with tectonic:

    tectonic project_status.tex

Produces `project_status.pdf` (14 frames, 16:9). Pure Beamer + TikZ + pgf math,
no external fonts, so it builds offline once the CTAN cache is warm.
